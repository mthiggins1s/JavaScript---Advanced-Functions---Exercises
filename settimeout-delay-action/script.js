// Using setTimeout to log a message after a delay
// Write your code below:

// Write a function that logs the message "Action executed!" to the console.
// Use setTimeout to delay this message by 3 seconds (3000 milliseconds).
// Test your code by running it in the browser console or a JavaScript environment.
// Hint: The first argument to setTimeout is the function to run, and the second argument is the delay in milliseconds.

function logMessage() {
    console.log("Action Executed!")
}

setTimeout(logMessage, 3000);