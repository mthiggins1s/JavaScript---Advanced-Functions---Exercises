// Objective: Write an arrow function that calculates the square of a number
    // Create an arrow function "square" that takes a number (x) as a parameter and returns the square of that number (x * x).
    // Call the square function with the number five and log the result.
// *Hint* Remember that with one parameter, you can omit the parentheses.

const square = x => x * x; // square will take 5 times itself.

console.log(square(5)); // calls the function with 5 times itself.